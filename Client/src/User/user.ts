export interface userState  {
    isLogined:boolean,
    accessToken: string,
    fullName?: string,
    email?: string,
    beer?: string,
    birthDate?: string,
    idNumber?: string
    favoriteFood?: Array<string>;
  };   
